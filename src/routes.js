import React from 'react'
import { Switch, Route, HashRouter } from 'react-router-dom'

export default (
    <HashRouter>
        <Switch>
            <Route></Route>
        </Switch>
    </HashRouter>
)